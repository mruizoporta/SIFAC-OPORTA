export interface categoryResponse {
    categoryid: number,
    name: string,
    description: string 
}

export interface PeriodicElement {
    name: string;
    position: number;
    weight: number;
    symbol: string;
  }